[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npm run build -w @myapps/api",
  "npm run build -w @myapps/web"
]

[start]
cmd = "cd apps/api && npx prisma migrate deploy && cd ../.. && NODE_ENV=production node apps/api/dist/index.js"
